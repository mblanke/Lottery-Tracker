
name: DoD Gate

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  dod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run DoD
        run: |
          chmod +x ./scripts/dod.sh || true
          ./scripts/dod.sh
